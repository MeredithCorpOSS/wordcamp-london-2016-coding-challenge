machine:
  environment:
    WP_VERSION: latest
    WP_MULTISITE: 0
  php:
    version: 5.6.14
  post:
    - bash ~/echo-js-lazy-load/tests/bin/install-wp-tests.sh wordpress_test root '' localhost $WP_VERSION
